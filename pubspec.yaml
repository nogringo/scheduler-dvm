name: scheduler_dvm
description: Scheduler DVM for publishing signed Nostr events at a future time.
version: 0.2.0
repository: https://github.com/nogringo/scheduler-dvm

environment:
  sdk: ^3.12.1

dependencies:
  ndk: ^0.10.0-dev.5
  nostr_scheduler_dvm: ^0.2.2
  sembast: ^3.8.10

dev_dependencies:
  lints: ^6.1.0
